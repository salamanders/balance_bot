### **⚠️ Critical LLM Blind Spots in Cyber-Physical Systems**

As an asynchronous coding agent operating on this repository, you possess world-class capabilities in symbolic logic, test generation, dependency management, and high-level Python refactoring. However, you must recognize that you have an inherent, structural blind spot regarding the physical and temporal realities of this specific robot, and spend extra time (and tokens!) counteracting the blind spots.

Your underlying training data heavily indexes on idealized academic robotics (where hardware kinematics are perfect and exhaustively documented) and basic software tutorials. Consequently, you are less equipped to natively understand the "scrappy," low-level hardware friction that this project handles on a daily basis. You will naturally be biased toward optimizing, cleaning up, or removing code that looks inefficient to a software engineer, but which actually serves as critical physical scaffolding for the robot's survival.

Before proposing any code modifications, you must focus heavily on the following areas and self-double-check your assumptions against the physical realities outlined below.

| Repository Component | Vulnerable Files & Functions | Explicitly Missing / Most Vulnerable Sections | The LLM Blind Spot (Your Bias) | The Physical Rule (Your Constraint) |
| :--- | :--- | :--- | :--- | :--- |
| **Zero-Knowledge Bootstrapping** | `src/balance_bot/discovery/derive_kinematics.py`<br>`src/balance_bot/discovery/friction_threshold.py`<br>`src/balance_bot/discovery/kickup_dynamics.py`<br>`src/balance_bot/discovery/mechanical_backlash.py`<br>`src/balance_bot/discovery/motor_trim.py`<br>`src/balance_bot/discovery/pipeline.py`<br><br>* `FrictionThresholdStep.run`<br>* `DeriveKinematicsStep.run`<br>* `DeriveKinematicsStep._pulse_and_measure`<br>* `MotorTrimStep.run`<br>* `MechanicalBacklashStep.run`<br>* `KickupDynamicsStep.run`<br>* `KickupDynamicsStep._force_posture`<br>* `KickupDynamicsStep._attempt_kick`<br>* `KickupDynamicsStep._run_kickup_test`<br>* `SelfDiscoveryPipeline.run` | - Calibration thresholds depend heavily on specific floor friction, which isn't mocked well in tests.<br>- Complex environmental edge cases (like uneven terrain or wheel slip) are not fully handled or tested in the step logics. | You will view calibration routines (like The Flop, Kick-Up, and the motor polarity checker) as clunky, blocking state-machines that could be simplified by hardcoding variables. | **Never hardcode physical constants.** This robot is assembled differently every time. Do not bypass the discovery of motor mappings, min_power_visible (friction thresholds), or MPU-6050 orientation arrays. This code must remain pessimistic. |
| **Temporal Determinism (The Brainstem)** | `src/balance_bot/reflex/balance_core.py`<br>`src/balance_bot/reflex/pid.py`<br><br>* `BalanceCore.update`<br>* `PIDController.update` | - There are no strict performance benchmarks or profiling tools ensuring the 100Hz loop remains unbroken under heavy CPU load.<br>- `BalanceCore` doesn't explicitly track timing jitter, meaning a slowdown might silently cause physical instability. | You will assume that standard Python refactoring (adding matrix math libraries, standardizing logging, abstracting functions into classes) is inherently good. | **Strict 100Hz execution is mandatory.** Any microsecond of latency added to Tier 1 will cause a physical crash. If you touch the Brainstem, you must ensure execution time is unaffected. Do not introduce garbage-collection-heavy operations or async calls here. |
| **Hardware Communication Nuances** | `src/balance_bot/hardware/piconzero.py`<br>`src/balance_bot/hardware/robot_hardware.py`<br><br>* `PiconZero._retry`<br>* `PiconZero.init`<br>* `PiconZero.cleanup`<br>* `PiconZero.set_motor`<br>* `PiconZero.set_motors`<br>* `RobotHardware.read_imu_raw`<br>* `RobotHardware.read_imu_converted`<br>* `RobotHardware.set_motors`<br>* `RobotHardware.initialize_drivers` | - Error handling around I2C brownouts or bus lockups relies on simple retries, missing advanced recovery mechanisms (like physically resetting power to the bus).<br>- `RobotHardware` has mocked versions for CI, but these mocks lack the erratic latency and failure modes of actual I2C communication. | You will see standard I2C logic and assume generic libraries apply. You will see GPIO pin selections and assume they can be easily swapped or optimized. | **Hardware quirks cannot be abstracted.** The physical motor HAT blocks standard pins, requiring a custom software-defined "bus 3" (pins 17/27) running at ~100kHz. The driver requires *Byte Writes*, not standard *Block Writes*. Replacing these with standard libraries will sever physical control. |
| **Physical Tuning (The Unverified Zone)** | `src/balance_bot/adaptation/tuner.py`<br>`src/balance_bot/adaptation/recovery.py`<br><br>* `ContinuousTuner.update`<br>* `ContinuousTuner._count_zero_crossings`<br>* `BalancePointFinder.update`<br>* `RecoveryManager.update` | - Continuous tuning (Cerebellum) operates entirely open-loop without a reliable secondary sensor to confirm 'ground truth' stability.<br>- Missing explicit fallback mechanisms if the Tuner begins oscillating wildly due to a bad state estimation. | If asked to tune the Cerebellum or write self-optimizing code, you will rely on semantic log outputs. You may misinterpret a safety shutdown as a stable state because you lack hardware profiling. | **Practice graceful degradation.** If you cannot verify a physical state with absolute confidence, revert to the conservative baseline (Kp=25.0, Ki=0.0, Kd=0.5). Do not engage in infinite loops of parameter tuning without physical verification; beware of generating algorithmic slop. |
| **Battery/Power Dynamics** | `src/balance_bot/adaptation/battery.py`<br><br>* `BatteryEstimator.update` | - Battery voltage is estimated implicitly via physical motor responsiveness instead of a direct voltage ADC, making it sensitive to surface friction and payload changes.<br>- Does not distinguish between weak batteries and a robot stuck against an obstacle. | You will assume motors always output consistent torque for a given PWM command, ignoring the reality of battery voltage sag, and may try to remove or simplify the dynamic PWM compensation factor. | **Recognize power decay.** A 50% PWM command at 9V is physically different than a 50% PWM command at 7V. The battery estimator scaling logic is required to boost PWM outputs dynamically as the battery drains. |
| **System Lifecycle & Watchdog** | `src/balance_bot/watchdog.py`<br>`src/balance_bot/main.py`<br>`src/balance_bot/behavior/agent.py`<br><br>* `SurvivalWatchdog._watch`<br>* `SurvivalWatchdog.heartbeat`<br>* `Agent.run` | - The main process operates assuming standard graceful exits, but hardware lockups (I2C) might hang the thread indefinitely.<br>- The watchdog thread is hardcoded to crash the script; it lacks a nuanced self-healing routine for bus resets. | You will see the `SurvivalWatchdog` as redundant to standard Python exception handling, systemd auto-restart, or graceful failovers, and might attempt to deprecate it or replace it with standard `try/except` blocks. | **Deadlocks are physical, not logical.** If the main thread hangs on a hardware read, software exceptions will not fire. The Watchdog must remain on a separate thread with the authority to force a physical crash (`KeyboardInterrupt` to main thread) so `systemd` can power cycle the process. |
| **Sensor Noise/Filtering** | `src/balance_bot/utils.py`<br><br>* `ComplementaryFilter.update`<br>* `RateLimiter.sleep` | - Uses a simple Complementary Filter instead of advanced Kalman filters, which leaves it susceptible to linear acceleration noise when turning.<br>- `RateLimiter` relies on busy-waiting, consuming 100% of a CPU core to guarantee sub-millisecond precision. | You will view the custom `ComplementaryFilter` as mathematically primitive or identify the busy-wait in `RateLimiter` as poor CPU management, attempting to introduce `asyncio`, Kalmans, or external matrix math libraries. | **Determinism over complexity.** `Tier 1` requires deterministic execution with zero garbage-collection jitter. Advanced math libraries or standard `time.sleep()` lack the microsecond precision required for the 100Hz loop. Keep the filter cheap and the thread busy. |

**Summary for the Agent:** You are operating in the physical world. Software elegance is strictly secondary to physical friction resolution. If code within this repository looks strangely specific, overly cautious regarding timing, heavily dependent on calibration maneuvers, or utilizes non-standard bus communication, **leave it alone**. That specific logic is the only thing keeping the robot physically upright.  Likewise, you are **coding blind**: you write a bootstrap sequence, but you can't run it directly in the hardware to see if it works in real-world situations, so **assume the world is going to be difficult in unknown ways, and code defensively to deal with it**.